import { TimelineDrawDirective } from './timeline-draw.directive';

describe('TimelineDrawDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineDrawDirective();
    expect(directive).toBeTruthy();
  });
});
