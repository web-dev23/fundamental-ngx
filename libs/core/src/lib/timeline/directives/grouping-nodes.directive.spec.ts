import { GroupingNodesDirective } from './grouping-nodes.directive';

describe('GroupingNodesDirective', () => {
  it('should create an instance', () => {
    const directive = new GroupingNodesDirective();
    expect(directive).toBeTruthy();
  });
});
